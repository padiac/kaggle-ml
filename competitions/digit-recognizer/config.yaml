competition_name: digit-recognizer
task: classification
target_column: label
id_column: ImageId

# Features will be auto-detected in train.py (pixel0..pixel783)
features:
  numeric: [] 
  categorical: []

model:
  name: random_forest
  params:
    n_estimators: 100
    n_jobs: -1
    random_state: 42

cv:
  n_splits: 3
  scoring: accuracy
